# Flask AI Model Server Dependencies
# These packages are required to run the AI prediction server

# Flask framework for creating the web API
Flask==2.3.3

# CORS support for cross-origin requests from Flutter app
Flask-CORS==4.0.0

# NumPy for array operations (required by the model)
numpy==1.24.3

# Scikit-learn for machine learning model support
scikit-learn==1.3.0

# Additional dependencies that might be needed
Werkzeug==2.3.7
